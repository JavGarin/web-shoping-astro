---
import Layout from '../../layouts/Layout.astro';
import { supabase } from '../../lib/supabase';
import type { Product } from '../../types';

export async function getStaticPaths() {
  const { data, error } = await supabase.from('products').select('id');
  if (error) {
    throw new Error(`Error fetching product IDs: ${error.message}`);
  }
  return data.map(({ id }) => ({
    params: { id: id.toString() },
  }));
}

const { id } = Astro.params;

const { data: product, error } = await supabase
  .from('products')
  .select('*')
  .eq('id', id)
  .single();

if (error || !product) {
  return Astro.redirect('/404');
}
---

<Layout title={product.name}>
  <section class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12">
    <div>
      <img
        src={product.imageUrl}
        alt={`Image of ${product.name}`}
        class="w-full h-auto object-cover rounded-lg shadow-lg"
      />
    </div>

    <div class="flex flex-col">
      <h1 class="text-4xl md:text-5xl font-bold text-charcoal-gray">{product.name}</h1>
      <p class="mt-4 text-3xl font-semibold text-forest-green">${product.price}</p>
      
      <div class="mt-6 border-t pt-6">
        <h2 class="text-xl font-semibold text-charcoal-gray">Description</h2>
        <p class="mt-2 text-charcoal-gray leading-relaxed">
          {product.description}
        </p>
      </div>

      <div class="mt-8 flex-grow flex items-end">
        <button
          type="button"
          id="add-to-cart-btn"
          class="w-full flex items-center justify-center gap-3 rounded-lg bg-forest-green px-6 py-4 text-lg font-medium text-cream-beige shadow-lg hover:bg-earth-brown transition-colors focus:outline-none focus:ring-2 focus:ring-golden-amber focus:ring-offset-2"
          data-product={JSON.stringify(product)}
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
          </svg>
          Add to Cart
        </button>
      </div>
    </div>
  </section>
</Layout>

<script>
  import { addToCart } from '../../lib/cart';

  function setupButton() {
    const button = document.getElementById('add-to-cart-btn');
    if (button && !button.dataset.listenerAttached) {
      button.addEventListener('click', () => {
        const product = JSON.parse(button.getAttribute('data-product'));
        addToCart(product);
      });
      button.dataset.listenerAttached = 'true';
    }
  }

  document.addEventListener('astro:page-load', setupButton);
  setupButton();
</script>